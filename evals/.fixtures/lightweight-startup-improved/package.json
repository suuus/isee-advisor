{
  "name": "tasks-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "fastify": "^4.28.0",
    "pg": "^8.12.0"
  }
}
