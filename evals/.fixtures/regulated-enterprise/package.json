{
  "name": "healthdata-platform",
  "version": "3.1.0",
  "scripts": {
    "dev": "fastify start -l info src/app.ts",
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": { "fastify": "^4.26.0", "pg": "^8.11.0" },
  "devDependencies": { "typescript": "^5.4.0", "jest": "^29.7.0" }
}
