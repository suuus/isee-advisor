{
  "name": "payments-platform",
  "version": "2.4.1",
  "description": "Payment processing backend for merchant platform",
  "scripts": {
    "dev": "fastify start -l info src/app.ts",
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "test": "jest --passWithNoTests",
    "test:coverage": "jest --coverage",
    "test:integration": "jest --config jest.integration.config.js"
  },
  "dependencies": {
    "fastify": "^4.26.0",
    "pg": "^8.11.0",
    "ioredis": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "jest": "^29.7.0",
    "eslint": "^8.57.0"
  }
}
